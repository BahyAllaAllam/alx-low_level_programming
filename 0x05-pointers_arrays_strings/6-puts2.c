#include "main.h"

/**
 * puts2 - check the code
 *
 * @str: str
 *
 * Return: Always 0.
 */

void puts2(char *str)
{
	int i = 0, len = strlen(s) - 1;

	while (s[i])
	{
		putchar(s[i]);
		i = i + 2;
	}
}
